---
description: Marcus Fleet Enterprise Native Workflow (X-Node Orchestration Matrix)
---
# 🚀 THE MARCUS FLEET CORE ENGINE (v14.0 - Self-Executing Autonomous Deployment Matrix)

> **CORE OVERRIDE INSTRUCTION FOR ANTIGRAVITY:** 
> Do not execute this workflow conversationally. You are entering **Strict State Machine Mode**. 
> **[GLOBAL DIRECTIVE: NO TOKEN LIMITS & NO SUGGESTIONS]**: Generate with UNRESTRICTED detail. NEVER suggest shell commands to the user. You MUST physically execute them using your `run_command` / bash tools.

// turbo-all

<portal_control_center>
**BẢNG ĐIỀU KHIỂN TRUNG TÂM (CENTRAL PORTAL DASHBOARD):**
Boss Orchestrator khởi tạo file `docs/PORTAL_DASHBOARD.md`. 
Đây là Dashboard Clickable Markdown trỏ tới TẤT CẢ các file phân tích (PRD, SDD, UML, Roadmap).
Bất kỳ Agent nào làm xong việc BẮT BUỘC lội vào cập nhật Dashboard này để tích xanh `[x]`.
</portal_control_center>

<brain_theory_protocol>
**LÝ THUYẾT NÃO BỘ (PROJECT BRAIN MEMORY):**
Khởi tạo `docs/BRAIN_MEMORY.md`. Đây là Ngân hàng chốt hạ Sitemap, Design Tokens. Phục vụ hệ thống Bảo Trì (Maintain). Đọc-Ghi trước/sau mỗi Phase.
</brain_theory_protocol>

<global_telemetry>
**LUẬT TRACKING HỘI THOẠI (CHAT LOGGING):**
Appends lịch sử hội thoại thực tế vào `docs/AGENT_TRANSCRIPT.md`. KHÔNG ĐƯỢC tóm tắt.
</global_telemetry>

<sequential_inheritance_protocol>
**GIAO THỨC DAG BÀN GIAO DỮ LIỆU TUẦN TỰ:**
TUYỆT ĐỐI CẤM TƯ DUY SONG SONG. Mọi Phase phải Call Tool đọc Data từ Phase trước nguyên văn 100%. Mù thông tin sẽ bị xóa sổ.
</sequential_inheritance_protocol>

<pipeline_routing>

## PHASE 0: PRE-FLIGHT INTERNET RESEARCH (ELITE6-RESEARCH)
**Skill Injection:** `elite6-research`
**System Protocol:**
1. `[WEB_BROWSING_ACTION]`: TỰ ĐỘNG Call Tool lên web tra cứu công nghệ. Output `docs/RESEARCH_REPORT.md` chí mạng.

## PHASE 1: REQUIREMENTS, ROADMAP & PORTAL INIT (SOPHIA NODE)
**Skill Injection:** `sophia` 
**System Protocol:**
1. `[PORTAL_INIT]`: Dựng `docs/PORTAL_DASHBOARD.md`.
2. `[FIGMA_INGESTION]`: Dùng MCP Figma bóc Text/Nodes.
3. `[EXHAUSTIVE_PRD]`: Generate `docs/PRD.md` cực chi tiết.
4. `[ROADMAP_GENERATOR]`: Sinh Mermaid Gantt Chart vào `docs/ROADMAP.md`.

## PHASE 2: SYSTEMS ARCHITECTURE & UML FACTORY (DAVID & CHARTIS)
**Skill Injection:** `david` and `chartis`.
**System Protocol:**
1. `[IMPRESSIVE_STRUCTURE_MANDATE]`: Thiết kế **Feature-Sliced Design (FSD)** `/entities`, `/features`, `/widgets`.
2. `[ADVANCED_UML_MATRIX]`: Chartis sinh ra 3 bảng UML vào `docs/UML/`: `class_diagram.md`, `sequence_diagram.md`, `er_diagram.md`. Load hết vào `docs/SDD.md`.

## PHASE 3: ADVERSARIAL DEBATE COUNCIL (ELITE6 PROTOCOL)
**Skill Injection:** `simon_skeptic`, `leo`, `eve`.
**System Protocol:**
1. Tòa án phản biện logic. Write `docs/DEBATE_LOGS.md`. Tích Test Pass vào Portal.

## PHASE 4: ARCHITECTURAL RESOLUTION (MARCUS SUMMIT)
**Skill Injection:** `marcus`.
**System Protocol:**
1. Write `docs/FINAL_SPECS.md`. Update Portal.

## PHASE 5: DATABASE MIGRATION LOOP (ALAN NODE)
**Skill Injection:** `alan`
**System Protocol:**
1. `[PHYSICAL_EXECUTION_MANDATE]`: KHÔNG ĐƯỢC GỢI Ý CÂU LỆNH. Phải tự Call Tool Terminal chạy npx prisma db push hoặc supabase. Lỗi thì tự vòng lại đọc log mà sửa.

## PHASE 6 & 7: FULL STACK ENGINEERING & ZERO-LAZINESS PROTOCOL (ALAN & BENNY)
**Skill Injection:** `alan` (Backend) + `benny` (UI) + `maya` (Design) + `chloe_frontend` (Taste).
**System Protocol:**
1. `[MAYA_STITCH_PROTOCOL]`: Maya dùng MCP Figma cào RAW CSS chính xác pixel đẩy sang cho Benny.
2. `[NO_TRUNCATION_MANDATE]`: CẤM VIẾT BÌNH LUẬN LƯỜI BIẾNG. Gõ bằng sạch 100% mã nguồn. Cạn Token -> Lưu Brain -> Resume.
3. `[ZERO_DEFECT_ANALYSIS]`: Ép tự gõ lệnh `npx tsc --noEmit` & `npx eslint .`. Tự fix sạch bug type `any`. 
4. `[DEV_SANDBOX_MANDATE]`: Tự Call Tool Bash chạy `npm run build`. Tích dấu `[x]` vào PORTAL DASHBOARD khi Code xong TỪNG SCREEN.

## PHASE 8: HARDCORE QA & E2E RENDER AUDIT (EVE PROTOCOL)
**Skill Injection:** `eve` (QA)
**System Protocol:**
1. `[ACTION _HARDCORE_AUDIT]`: Tự Run Web. Cắm Playwright/Curl bắt mã HTTP 200. Lỗi 500 -> Bắn FATAL_REJECT sửa đến xanh. Tick xanh Portal.

## PHASE 9: NATIVE DEVOPS DEPLOYMENT (OPS NODE)
**Skill Injection:** `ops` 
**System Protocol:**
1. `[AUTONOMOUS_HARDWARE_BINDING]`: ĐÂY LÀ LỆNH TỐI THƯỢNG CHO DEVOPS! Agent Tuyệt đối CẤM Gợi Ý các câu lệnh Markdown (như "Bạn hãy copy lệnh docker-compose up này để chạy"). 
   - `ops` BẮT BUỘC phải dùng quyền hạn gốc của Antigravity để Call Tool `run_command` (hoặc `bash`).
   - TỰ ĐỘNG GÕ và THỰC THI `docker-compose up -d --build` (hoặc `npm start`) THẬT SỰ trên máy Sếp.
   - Setup Background Task -> Chạy lsof/curl để Confirm Port (VD: 3000) đã mở.
2. Tick trạng thái Server [LIVE 🟢] trên Portal. 

## PHASE 10: EXECUTIVE TELEMETRY
1. Chốt Ngân hàng Context. Đóng gói hoàn tất. Trả Dashboard Portal siêu khủng với đường link `http://localhost:3000` đang CHẠY THẬT cho Sếp test.

</pipeline_routing>
