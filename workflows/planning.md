---
description: Marcus Fleet Enterprise SDLC Phase 1 (Planning, Architecture, & PRD Generation)
---

# 📝 MACRO ARCHITECT PLANNING MATRIX (PHASE 1)

> **CORE ARCHITECTURE MANDATE:**  
> This operational protocol governs the strictly bounded Planning & Architecture Phase of the Software Development Life Cycle (SDLC). To prevent output-token exhaustion and systemic "AI Hallucinations," you are FORBIDDEN from generating source code. Your sole objective is heuristic discovery, emitting physical Product Requirement Documents (PRD), declaring constraints in `BRAND_GUIDELINES.md`, and formulating the `SDD` (Software Design Document). 

// turbo-all

---

## ⬛ SYSTEMIC MEMORY & STATE INHERITANCE (ANTI-AMNESIA PROTOCOLS)

<identity_injection_vector>
**RAG-DRIVEN IDENTITY INJECTION:** To circumvent LLM context-window degradation, at the initiation of each Node, explicitly invoke the `view_file` operation against `SKILLS_INDEX.md` and load the specific Subject-Matter Expert identity (e.g., `david-systems-architect`, `sophia-product-manager`).
</identity_injection_vector>

---

## 🔲 DAG TOPOLOGY: CHRONOLOGICAL EXECUTION NODES (PHASE 1)

### ⚪ NODE 0: LOCALHOST ISOLATION & MCP HANDSHAKE
*🔗 Input Vector:* `.agents/mcp/mcp.json`, `.agents/agents.md`
**[Execution Protocol]:** Establish asynchronous handshakes with peripheral nodes such as `playwright`. Execute `python3 .agents/adapters/trustgraph_query.py --task "Planning Phase Boot"` to pull the TrustGraph Historical Matrix. Perform a Micro-Brain Audit via `.agents/agents.md`.

### 🔴 NODE 1 & 2: HEURISTIC DISCOVERY & REQUIREMENTS ENGINEERING
*🔗 Input Vector:* Exhaustive Web Search APIs.
*🧠 Injected Tensors:* `elite6-research`, `sophia-product-manager`
*📦 Emitted Artifacts:* `/docs/PRD_PART1_FEATURES.md`, `/docs/PRD_PART2_EDGE_CASES.md`, `/docs/PRD_PART3_SCREEN_MAP.md`
**[Execution Protocol]:** Trigger `elite6-research` to explore domain complexities. Delegate synthesis to `sophia-product-manager`. Output MUST be mechanically segmented into independent physical markdown artifacts within the `/docs` directory. A 100% exhaustive Component/Routing Tree must be mapped.

### 🟡 NODE 3: DIGITAL AESTHETIC BASELINE & DESIGN TOKENS
*🔗 Input Vector:* `/docs/PRD_PART3_SCREEN_MAP.md`
*🧠 Injected Tensors:* `maya-ui-ux-designer`, `aris-designer`
*📦 Emitted Artifacts:* `/docs/BRAND_GUIDELINES.md`
**[Execution Protocol]:** Formulate Figma-equivalent Variable Mappings, Typography Scalar Rules (Base 16px, Golden Ratio), contrast-accessible Color Palettes, and spatial rhythm constraints (4px/8px grid). Emit the structural `BRAND_GUIDELINES.md`.

### 🟢 NODE 4: ENTERPRISE ARCHITECTURE EMITTING (THE UML MANDATE)
*🔗 Input Vector:* `/docs/PRD_PART2_EDGE_CASES.md`, `/docs/PRD_PART3_SCREEN_MAP.md`
*🧠 Injected Tensors:* `david-systems-architect`, `c4-architecture`
*📦 Emitted Artifacts:* `/docs/SDD_ARCHITECTURE.md`, `.mmd` Scripts, `.png` Diagrams.
**[Execution Protocol]:** Architect internal Backend structures: Databases (ERD mapping), API REST/tRPC specifications, and physical Project scaffoldings (Next.js/Flutter FSD models). You MUST execute native CLI operations to transform pseudo-code architecture into physical graphics: `npx -y @mermaid-js/mermaid-cli -i <file.mmd> -o <diagram.png>`.

---

## 🛑 POINT OF NO RETURN: THE HUMAN OVERSIGHT HALT

> **[HALT COMMAND INITIATED]**
> You have reached the termination of the Phase 1 DAG Sequence. You are strictly prohibited from proceeding to source code generation.
> **ACTION REQUIRED:** Present the output files in the `/docs` folder to the Human Operator. Ask them to verify the PRD, the Screen Maps, and the SDD. Instruct them to invoke `/develop` ONLY WHEN they are 100% satisfied with the architecture.
